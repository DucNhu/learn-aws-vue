type TagModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
}

type PhotoModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  desciption: String
  tagId: String
  view: Int
  userId: String
  status: Boolean
}
 